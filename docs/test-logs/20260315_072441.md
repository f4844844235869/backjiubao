# 测试日志

记录时间：`2026-03-15 07:24:41 CST`

## 本轮目标

- 补齐员工访问小程序接口边界测试
- 补齐角色解绑权限后即时失效测试
- 更新场景清单和并行执行说明

## 执行命令

```bash
cd /Users/yimo/Desktop/codex/full-stack-fastapi-template/backend
uv run ruff check app tests scripts
uv run pytest tests/api/routes/test_employee.py tests/api/routes/test_org_iam.py -q
```

## 关键接口响应

| 步骤 | 接口 | 期望 | 实际 |
| --- | --- | --- | --- |
| 1 | `POST /api/v1/auth/backend/password-login` | 员工账号登录成功 | `200 SUCCESS` |
| 2 | `GET /api/v1/auth/miniapp/phone-related-employees` | 员工账号访问小程序手机号关联接口被拒绝 | `403 AUTH_PERMISSION_DENIED` |
| 3 | `POST /api/v1/iam/roles` | 创建角色成功 | `201 SUCCESS` |
| 4 | `GET /api/v1/iam/permissions` | 获取权限列表成功 | `200 SUCCESS` |
| 5 | `PUT /api/v1/iam/roles/{role_id}/permissions` | 先绑定 `org.store.read` 成功 | `200 SUCCESS` |
| 6 | `PUT /api/v1/iam/users/{user_id}/roles` | 给目标用户分配角色成功 | `200 SUCCESS` |
| 7 | `PUT /api/v1/iam/users/{user_id}/data-scopes` | 给目标用户分配门店范围成功 | `200 SUCCESS` |
| 8 | `GET /api/v1/stores/` | 解绑前可正常读取门店列表 | `200 SUCCESS` |
| 9 | `PUT /api/v1/iam/roles/{role_id}/permissions` | 将角色权限替换为空，完成解绑 | `200 SUCCESS` |
| 10 | `GET /api/v1/stores/` | 解绑后旧 token 再访问被拒绝 | `403 AUTH_PERMISSION_DENIED` |

## 测试结果

- `ruff` 检查：通过
- 定向接口测试：`12 passed`
- warning：`2` 个，均为本地默认开发配置提醒

## 结论

- 员工账号不能直接调用仅限小程序用户的手机号关联接口
- 角色权限解绑后会立即生效，不依赖重新登录
- 当前这类权限回收测试已经能覆盖“授权变化即时生效”的关键链路
- 测试仍建议默认串行执行；如果未来要并行，需要给并行进程准备独立测试库
