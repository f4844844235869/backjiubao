# 测试日志

记录时间：`2026-03-15 07:29:48 CST`

## 本轮目标

- 补齐“离职员工再次入职后不自动恢复权限”的业务规则测试
- 明确复职必须重新授权的系统行为

## 执行命令

```bash
cd /Users/yimo/Desktop/codex/full-stack-fastapi-template/backend
uv run ruff check app tests scripts
uv run pytest tests/scenarios/test_employee_rehire_flow.py -q
```

## 关键接口响应

| 步骤 | 接口 | 期望 | 实际 |
| --- | --- | --- | --- |
| 1 | `POST /api/v1/employees/onboard` | 首次入职时带角色和门店数据范围成功 | `201 SUCCESS` |
| 2 | `GET /api/v1/stores/` | 首次入职员工可读取门店列表 | `200 SUCCESS` |
| 3 | `POST /api/v1/employees/{user_id}/leave` | 员工离职成功 | `200 SUCCESS` |
| 4 | `POST /api/v1/employees/onboard` | 同手机号重新入职成功，生成新员工账号 | `201 SUCCESS` |
| 5 | `GET /api/v1/stores/` | 再次入职后未重新授权，访问门店列表被拒绝 | `403 AUTH_PERMISSION_DENIED` |
| 6 | `PUT /api/v1/iam/users/{user_id}/roles` | 重新分配角色成功 | `200 SUCCESS` |
| 7 | `PUT /api/v1/iam/users/{user_id}/data-scopes` | 重新分配数据范围成功 | `200 SUCCESS` |
| 8 | `GET /api/v1/stores/` | 重新授权后恢复门店访问能力 | `200 SUCCESS` |

## 测试结果

- `ruff` 检查：通过
- 复职场景测试：`1 passed`
- warning：`2` 个，均为本地默认开发配置提醒

## 结论

- 离职员工再次入职时不会继承历史角色和数据范围
- 复职按新员工处理，必须重新授权后才能恢复原有可访问接口
- 这条规则已经通过自动化场景测试固化
