# 测试日志

记录时间：`2026-03-15 07:30:41 CST`

## 本轮目标

- 补齐角色修改与角色删除接口测试
- 补齐员工岗位变更场景测试
- 将新场景接入自动场景执行脚本

## 执行命令

```bash
cd /Users/yimo/Desktop/codex/full-stack-fastapi-template/backend
uv run ruff check app tests scripts
uv run pytest tests/api/routes/test_org_iam.py tests/scenarios/test_employee_position_change_flow.py -q
```

## 关键接口响应

| 步骤 | 接口 | 期望 | 实际 |
| --- | --- | --- | --- |
| 1 | `PATCH /api/v1/iam/roles/{role_id}` | 角色编码、名称、状态更新成功 | `200 SUCCESS` |
| 2 | `DELETE /api/v1/iam/roles/{role_id}` | 普通角色删除成功 | `200 SUCCESS` |
| 3 | `GET /api/v1/iam/roles` | 已删除角色不再出现在角色列表 | `200 SUCCESS` |
| 4 | `POST /api/v1/employees/onboard` | 员工入职成功，初始岗位写入主组织绑定 | `201 SUCCESS` |
| 5 | `PATCH /api/v1/org/bindings/{binding_id}` | 主组织绑定岗位名称更新成功 | `200 SUCCESS` |
| 6 | `GET /api/v1/org/bindings?user_id={user_id}` | 组织绑定中的岗位名称已更新 | `200 SUCCESS` |
| 7 | `GET /api/v1/employees/{user_id}/employment-records` | 当前任职记录中的岗位名称同步更新 | `200 SUCCESS` |

## 测试结果

- `ruff` 检查：通过
- 定向测试：`9 passed`
- warning：`2` 个，均为本地默认开发配置提醒

## 结论

- 角色维护已经覆盖创建、修改、删除、分配权限、解绑权限这条主链路
- 员工岗位变更现在不只是改组织绑定，当前任职记录也会同步更新
- 新岗位变更场景已经加入场景执行脚本，可被 `backend`、`permission`、`all` 分组自动串行执行
