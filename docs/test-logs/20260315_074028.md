# 测试日志

记录时间：`2026-03-15 07:40:28 CST`

## 本轮目标

- 补齐门店更新/删除接口
- 补齐组织节点更新/删除接口
- 固化“在用不可删、历史仅软停用、空数据可物理删除”的删除规则

## 执行命令

```bash
cd /Users/yimo/Desktop/codex/full-stack-fastapi-template/backend
uv run ruff check app tests scripts
uv run pytest tests/api/routes/test_org_iam.py tests/scenarios/test_store_org_maintenance_flow.py tests/scenarios/test_store_org_deletion_boundary_flow.py -q
```

## 关键接口响应

| 步骤 | 接口 | 期望 | 实际 |
| --- | --- | --- | --- |
| 1 | `PATCH /api/v1/stores/{store_id}` | 更新门店名称/状态成功 | `200 SUCCESS` |
| 2 | `DELETE /api/v1/stores/{store_id}` | 无引用门店物理删除成功 | `200 SUCCESS` |
| 3 | `DELETE /api/v1/stores/{store_id}` | 在用门店删除被拒绝 | `409 STORE_IN_USE` |
| 4 | `DELETE /api/v1/stores/{store_id}` | 仅历史引用门店改为停用 | `200 SUCCESS` |
| 5 | `PATCH /api/v1/org/nodes/{node_id}` | 更新组织名称/排序/启停成功 | `200 SUCCESS` |
| 6 | `DELETE /api/v1/org/nodes/{node_id}` | 无引用组织物理删除成功 | `200 SUCCESS` |
| 7 | `DELETE /api/v1/org/nodes/{node_id}` | 在用组织删除被拒绝 | `409 ORG_NODE_IN_USE` |
| 8 | `DELETE /api/v1/org/nodes/{node_id}` | 仅历史引用组织改为停用 | `200 SUCCESS` |
| 9 | `DELETE /api/v1/org/nodes/{node_id}` | 存在子节点的组织删除被拒绝 | `409 ORG_NODE_IN_USE` |

## 测试结果

- `ruff` 检查：通过
- 定向测试：`20 passed`
- warning：`2` 个，均为本地默认开发配置提醒

## 结论

- 门店与组织节点都已支持基础维护
- 删除规则已经统一成三档：在用拦截、历史软停用、空数据物理删除
- 这两条场景已经接入自动场景执行脚本，可随 `backend`、`permission`、`all` 分组串行执行
